apiVersion: apps/v1
kind: Deployment
metadata:
  name: register-app-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: register-app1
  template:
    metadata:
      labels:
        app: register-app1
    spec:
      containers:
        - name: register-app1
          image: ashfaque9x/register-app-pipeline:1.0.0-17
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
